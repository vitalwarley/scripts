# TODO:
# remove ",destaque" words
# remove header
# remove first column (regex like '^\.,', anything from start to first comma)
# add a "* " at the begin of each line (inserte in the regex, maybe with sed)
